name: Github Checkout Demo
on: push
jobs:
  action-demo:
    runs-on: ubuntu-latest
    steps:
      - name: code checkout
        uses: actions/checkout@v5
      - name: Print index.html file
        run: cat index.html
        
    
  
