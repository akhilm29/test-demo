name: Hello world2
on: workflow_dispatch
jobs:
    build:
       runs-on: ubuntu-latest
       steps:
         - name: Build
           run: echo "A demo build"
    test:
        runs-on: ubuntu-latest
        steps:
          - name: Test
            run: echo "Running Test cases"
    deploy: 
        runs-on: ubuntu-latest
        steps:
          - name: Deploy
            run: echo "Deployed thr app"
            
